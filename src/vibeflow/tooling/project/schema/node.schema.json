{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vibeflow.local/schema/node.schema.json",
  "title": "VibeFlow Node",
  "type": "object",
  "required": ["NODE_INFO", "CONTRACT", "run_pure"],
  "properties": {
    "NODE_INFO": {
      "type": "object",
      "required": ["type_key", "display_name", "category", "description", "version", "flow_kind"],
      "properties": {
        "type_key": { "type": "string", "minLength": 1 },
        "display_name": { "type": "string", "minLength": 1 },
        "category": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "minLength": 1 },
        "flow_kind": { "enum": ["terminal", "process", "decision", "io", "predefined", "data_store", "document", "preparation", "global_state"] },
        "external": { "type": "boolean" }
      }
    },
    "CONTRACT": {
      "$ref": "#/$defs/contract"
    },
    "run_pure": {
      "description": "Python method with signature run_pure(self, inputs, params).",
      "type": "string"
    }
  },
  "$defs": {
    "contract": {
      "type": "object",
      "required": ["requires", "provides", "input_semantics", "output_semantics"],
      "properties": {
        "requires": { "type": "array", "items": { "$ref": "#/$defs/dataRequirement" } },
        "provides": { "type": "array", "items": { "$ref": "#/$defs/dataProvider" } },
        "input_semantics": { "type": "object" },
        "output_semantics": { "type": "object" },
        "examples": { "type": "array", "items": { "type": "object" } }
      }
    },
    "dataProvider": {
      "type": "object",
      "required": ["key", "type", "display_name"],
      "properties": {
        "key": { "type": "string", "minLength": 1 },
        "type": { "type": "string", "minLength": 1 },
        "display_name": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "dataRequirement": {
      "type": "object",
      "required": ["type", "cardinality", "display_name"],
      "properties": {
        "type": { "type": "string", "minLength": 1 },
        "cardinality": { "enum": ["exactly_one", "optional_one", "all"] },
        "display_name": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    }
  }
}
