{
  "pipeline": {
    "inputs": [
      {
        "key": "x",
        "type": "sandbox.number",
        "display_name": "Number",
        "required": true
      }
    ],
    "nodes": [
      {
        "id": "start",
        "type_used": "sandbox.terminal",
        "display_name": "Start",
        "description": "Starts the isolated runtime import fixture."
      },
      {
        "id": "invalid",
        "type_used": "sandbox.invalid.runtime_import",
        "display_name": "Runtime import",
        "description": "Must fail because the local helper is not a registered resource owner."
              },
      {
        "id": "end",
        "type_used": "sandbox.terminal",
        "display_name": "End",
        "description": "Ends the isolated runtime import fixture."
      }
    ],
    "edges": [
      {"from": "start", "to": "invalid"},
      {"from": "invalid", "to": "end"}
    ],
    "outputs": [
      {
        "type": "sandbox.math.result",
        "cardinality": "exactly_one",
        "display_name": "Result",
        "as": "result"
      }
    ]
  }
}
