{
  "pipeline": {
    "inputs": [
      {
        "key": "x",
        "type": "sandbox.x",
        "display_name": "X",
        "required": true
      }
    ],
    "nodes": [
      {
        "id": "start",
        "type_used": "sandbox.terminal",
        "display_name": "Start",
        "description": "Starts both the data source and control-delay branches."
      },
      {
        "id": "seed",
        "type_used": "sandbox.fanout_add",
        "display_name": "Seed payload",
        "description": "Adds one to x and transfers the result without scheduling the consumer.",
        "requires": [
          {
            "type": "sandbox.x",
            "cardinality": "exactly_one",
            "display_name": "X"
          }
        ],
        "provides": [
          {
            "key": "added_branch",
            "type": "sandbox.fanout.added",
            "display_name": "Added payload"
          }
        ]
      },
      {
        "id": "delay",
        "type_used": "sandbox.terminal",
        "display_name": "Control delay",
        "description": "Keeps the consumer off the schedule until the poison node runs."
      },
      {
        "id": "poison",
        "type_used": "sandbox.edge_poison",
        "display_name": "Produce sentinel",
        "description": "Produces a same-typed sentinel that must not reach the consumer.",
        "provides": [
          {
            "key": "poisoned",
            "type": "sandbox.fanout.added",
            "display_name": "Poisoned payload"
          }
        ],
        "value": 999
      },
      {
        "id": "consume",
        "type_used": "sandbox.edge_consume",
        "display_name": "Consume transferred payload",
        "description": "Runs only after poison schedules it, then doubles the seed payload.",
        "requires": [
          {
            "type": "sandbox.fanout.added",
            "cardinality": "exactly_one",
            "display_name": "Transferred payload"
          }
        ],
        "provides": [
          {
            "key": "result",
            "type": "sandbox.math.result",
            "display_name": "Result"
          }
        ]
      },
      {
        "id": "end",
        "type_used": "sandbox.terminal",
        "display_name": "End",
        "description": "Ends after the consumer result is available."
      }
    ],
    "edges": [
      {"from": "start", "to": "seed"},
      {"from": "start", "to": "delay"},
      {"from": "seed", "to": "consume", "schedule": false},
      {"from": "delay", "to": "poison"},
      {"from": "poison", "to": "consume", "transfer": false},
      {"from": "consume", "to": "end"}
    ],
    "outputs": [
      {
        "type": "sandbox.math.result",
        "cardinality": "exactly_one",
        "display_name": "Result",
        "as": "result"
      }
    ]
  }
}
