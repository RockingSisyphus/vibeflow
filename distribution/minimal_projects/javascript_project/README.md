# Minimal JavaScript AOT project

The workflow is `input boundary -> business process -> output boundary`, with explicit start and end terminals. Replace the pass-through under `BUSINESS CODE` in `nodes/process_payload.ts`, then update its descriptor if the data relationship changes. VibeFlow does not invent a domain model or business assertions.
