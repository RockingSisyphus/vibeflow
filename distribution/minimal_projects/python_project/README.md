# Minimal Python Runtime project

The workflow is `input boundary -> business process -> output boundary`, with explicit start and end terminals. Replace the pass-through under `BUSINESS CODE` in `nodes/workflow_nodes.py`, then update its Node contract if the data relationship changes. VibeFlow does not invent a domain model or business assertions.
