# 00. 内核目的与项目结构

VibeFlow 的目的不是让开发者写得更自由，而是故意限制程序结构，强制程序长期保持低耦合、可审计、可维护。

它适合人机协同开发，尤其适合让 LLM 参与长期项目开发。LLM 很容易把功能越写越大，或者在出问题时用补丁覆盖问题。这个内核要求所有业务能力都拆成小型纯函数 node，并用 JSONC 配置组织成标准流程图。只要 node 或配置违反规则，运行前健康检查会拒绝执行并给出原因。

## 使用者需要开发什么

通常只需要开发这些目录：

```text
project/
  nodes/          # 业务 node，只允许纯函数逻辑
  base_lib/       # 可复用纯函数 helper
  plugins/        # policy/compiler/runtime 插件
  configs/        # JSONC 拓扑配置和 nodeset 定义
  registry.py     # 注册 node
```

内核目录建议放在：

```text
kernel/vibeflow/
```

如果需要 SVG 图输出，分发包还会包含：

```text
tools/mermaid-renderer/
```

启动脚本建议放在项目根目录：

```text
run.py
```

## 标准工作流

1. 写一个 node，补全 `NODE_INFO`、`CONTRACT`、`run_pure(self, inputs, params)`。
2. 每个 node 必须声明标准 `flow_kind`。
3. 在 `project/registry.py` 注册 node，同时提供配置 schema 和默认值。
4. 在 `project/configs/*.jsonc` 中用不同 `name` 调用 node。
5. 用显式 `pipeline.edges` 组织 flow，不依赖 `requires/provides` 推导控制流。
6. 如果需要组合大模块，写 `nodesets`，再用 `type: "nodeset.xxx"` 调用。
7. 运行健康检查。
8. 只有健康检查通过后，才执行运行。
9. 导出 Mermaid / ASCII / SVG 图审查整体结构。

## 常用命令

在复制了模板的项目根目录运行：

```powershell
python run.py validate --config project/configs/main.jsonc
python run.py run --config project/configs/main.jsonc --run-root runs
python run.py mermaid --config project/configs/main.jsonc --output reports/graph.mmd
python run.py ascii --config project/configs/main.jsonc --output reports/graph.txt
python run.py svg --config project/configs/main.jsonc --output reports/graph.svg
python run.py inspect-config --config project/configs/main.jsonc
python run.py quality --path project
```

首次使用 `svg` 命令前，需要在 `tools/mermaid-renderer/` 执行 `npm install`。

`validate` 和 `run` 都会执行强制健康检查。`run` 在检查失败时会拒绝运行，并把报告写入运行目录。

## 关键变化

- 旧 `boundary` 模型已移除。
- 旧 `pipeline.loops` / `max_iterations` / edge `max_executions` 已移除。
- 外部输入输出通过 `io` 节点建模。
- 数据存储请求通过 `data_store` 节点建模。
- 文档产物通过 `document` 节点建模。
- 第三方或外部维护代码用 `NodeInfo.external=True` 标记。
